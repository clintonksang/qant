version: '3.8'

services:
  quant-agent:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./data:/app/data
      - ./assets:/app/assets
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped


